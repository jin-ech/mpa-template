import React from "react";

import './index.less';

const PREFIX = 'layout-wrapper';

const Layout = () => {

    return (
        <div className={PREFIX}>
            <h1>module2</h1>
        </div>
    );
};

export default Layout